import { LastUpdatePipe } from './last-update.pipe';

describe('LastUpdatePipe', () => {
  it('create an instance', () => {
    const pipe = new LastUpdatePipe();
    expect(pipe).toBeTruthy();
  });
});
